use crate::*;

puzzle!("Day -: ---", Solver, 0, 0);

struct Solver {}

impl Solver {
    fn ingest(input: &str) -> Self {
        Self {}
    }

    fn part_one(self) -> usize {
        0
    }

    fn part_two(self) -> usize {
        0
    }
}
